// TODO: have another go at this one

function substrings(string) {
  const newArray = [];
  let letters = string.split("");
  letters.forEach((letter, index) => {});
  return newArray;
}

substrings("ab"); //?
// newArray.push(letters.slice(0, index + 1).join(""));

// returns
// [ "a", "ab", "abc", "abcd", "abcde",
//   "b", "bc", "bcd", "bcde",
//   "c", "cd", "cde",
//   "d", "de",
//   "e" ]
